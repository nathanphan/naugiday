# Contracts

No new external APIs or backend contracts are introduced for this UI/UX improvements feature. All changes are within the Flutter presentation layer. If future API calls are added (e.g., share endpoints or AI backends), document them here with request/response schemas.
